#include <bits/stdc++.h>
#include <cstdio.h>
int main()
{
	int t;
	scanf("%d", &t);
	while (t--)
	{
		unsigned long long n;
		scanf("%llu", &n);
		vector<unsigned long long> a(n), dp(n);
		for (int i = 0; i < n; i++)
			scanf("%llu", &v[i]);
		dp[0] = a[0];
		for (int i = 1; i < n; i++)
		{
			if (dp[i] )
		}
	}
	return 0;
}
